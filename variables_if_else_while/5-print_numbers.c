#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints lowercase and uppercase alphabet
 * Return: Always 0 (Success)
 */

int main(void)
{
char ch = 1;
for (ch = 1; ch < 10; ch++)
{
printf("%d", ch);
}
printf("\n");
return (0);
}
