#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints lowercase and uppercase alphabet
 * Return: Always 0 (Success)
 */

int main(void)
{
char ch = '1';
for (ch = '1'; ch <= '9'; ch++)
putchar(ch);
return (0);
}
